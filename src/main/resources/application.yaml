spring:
  application:
    name: equipo-futbol-backend

  datasource:
    url: jdbc:mysql://localhost:3306/futbol_db
    username: java
    password: Theburgery1!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true

server:
  port: 8080
  servlet:
    context-path: /api/v1/equipo

logging:
  level:
    "[org.springframework]": INFO

security:
  jwt:
    secret-key: DxUw7ivIZN+DhQSvueypdE+9ATSldiDQ2HlPvqfRiZw=
    token-expiration: 600000
